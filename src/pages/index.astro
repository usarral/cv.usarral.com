---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import {NAME, SUBTITLE, PERSONAL_DESCRIPTION, PROJECTS} from "@data/config"

---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5 content-center">
    <div class="text-xl py-1">Hey there 👋</div>
    <div class="text-5xl font-bold">I'm {NAME}</div>
    <div class="text-3xl py-3 font-bold">{SUBTITLE}</div>
    <div class="py-2">
      <text class="text-lg">
        {PERSONAL_DESCRIPTION}
      </text>
    </div>
    <div class="mt-8">
      <a class="btn" href="mailto:carlos@usarral.com" target="_blank"> Let's talk!</a>
      <a href="/projects" target="_blank" class="btn btn-outline ml-5">
        Discover my projects
      </a>
    </div>
  </div>

  {
/* <div>
<div class="text-3xl w-full font-bold mb-2">My last projects {"</>"}</div>
</div>
{PROJECTS.sort((a, b) => new Date(b.postDate) - new Date(a.postDate)).slice(0, 3).map((project) => (
<>
<HorizontalCard
title={project.title}
img={project.image}
desc={project.description}
url={project.link}
badge={project.techs}
/>
<div class="divider my-0"></div>
</>
))}
*/
  }
 
</BaseLayout>
