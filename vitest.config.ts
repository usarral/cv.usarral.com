{
  "name": "vitest.config.ts",
  "content": "import { Config } from 'vitest';\n\nconst config: Config = {\n  testMatch: ['**/*.test.ts'],\n  setupFilesAfterEnv: [],\n};\n\nexport default config;\n"
}